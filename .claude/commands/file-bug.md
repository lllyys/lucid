---
description: "Create a GH issue for a bug row in docs/bugs.md and stamp `GH: #N` into its Notes column"
argument-hint: "<bug-id>"
---

# File Bug Issue

Usage: `/file-bug <bug-id>` — e.g. `/file-bug 115`.

Invoke the `file-bug` skill with `$ARGUMENTS`. The full procedure (pre-flight, issue body, row stamp, failure modes) lives in `.claude/skills/file-bug/SKILL.md` — single source per `.claude/rules/20-logging-and-docs.md`.
