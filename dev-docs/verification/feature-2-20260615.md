---
feature: 2
title: Lucid Workspace (Translate + Polish)
status: VERIFIED
result: pass
date: 2026-06-15
verifier: Claude (pnpm check:all + behavioral/integration tests against a mocked provider)
final_wi: WI-9
---

# Feature #2 — Lucid Workspace: acceptance evidence

Mock-verified per the user's Gate-2 Decision A: the workspace is exercised end-to-end against a
**mocked provider** (rule 65 §8 — never a live API). Real-key end-to-end is blocked on the
key-entry design (needs-design #13) and is recorded under Deferred.

## Acceptance criteria → evidence

| Criterion (plan DoD) | Result | Evidence |
|---|---|---|
| All logic TDD-built; 100% coverage on `providers`/`lib`/`stores`; `pnpm check:all` green | pass | `pnpm check:all` clean; **352 tests / 24 files**; coverage 100% (495/495 stmts, 371/371 branches, 84/84 funcs). Commit `6a8d62c`. |
| WI-2 additive provider/prompt change leaves feature-#1 behavior unchanged | pass | `prompts/index.test.ts` asserts byte-identical polish prompt when `original`/`keywords` absent; `base.test.ts` asserts `collectStream` unchanged after the `mapStreamError` extraction; all pre-existing provider tests green. |
| Stream a two-way 中↔EN translation (live caret + e2e timer; Stop → clean cancelled keeping partial text) | pass | `TranslatePanel.test.tsx` (direction detection, Run streams via mocked provider, Copy/Accept on done); `operationStore.test.ts` (abort → cancelled keeping partial text + frozen elapsed); `useElapsedTimer.test.tsx` (live tick). |
| Polish a draft against original + domain keywords; Result/Compare over a real word-diff; Accept commits the whole result to the draft | pass | `PolishPanel.test.tsx` (translate-original→draft, polish→result, Compare toggle, Accept commits + toast); `wordDiff.test.ts` (whole-accept reproduces result exactly — rule 66 §2); `integration.test.ts` (polish-reference JSON contract end-to-end). |
| Switch among implemented providers; provider-aware privacy line | pass | `ProviderSwitcher.test.tsx` (implemented-only menu, no silent no-op); `FooterPrivacy.test.tsx` (hosted/local lines). |
| On a mid-stream error the partial text stays (no raw leak); cancelled shows no message | pass | `operationStore.test.ts` (error keeps partial text from the normalized outcome; cancelled carries no error); `integration.test.ts` (429 → localized message, no raw payload). |
| No component calls a vendor/`fetch`; store maps no errors; display via `providerPresentation`; strings via `t()`; `.dark` mechanism present; focus visible | pass | UI talks only to `usePanelRun`→`operationStore`→`provider.streamOp`; `providerPresentation.test.ts`; i18n key-completeness test; `index.css` `.dark` scope; native focus on editors (rule 51-permitted). |
| needs-design issues filed; no placeholder/invented UI | pass | #13–#18 filed; sidebar/error-message/dark/RTL/Reject/key-entry surfaces not invented (rule 51). |
| #2 not end-to-end usable with a real key until key-entry is designed — stated, not silent | pass | Documented here + in the plan; key-entry is needs-design #13. |

## Method

- `pnpm check:all` = `lint` → `typecheck` → `test:coverage` (100% thresholds on the logic globs) → `build`. Green.
- Behavioral component tests (`@testing-library/react` + `userEvent`, ARIA-role queries) and the
  provider-stack integration test run against a **mocked** provider/`fetch` (rule 65 §8). No live API.
- Phase-0 spike (`dev-docs/grills/feature-2/PHASE-0.md`) validated the jsdiff/`Intl.Segmenter`
  assumptions and pinned the 30k-char diff threshold before WI-1.

## Deferred

- **Real-key end-to-end + browser/Playwright E2E** — blocked on the API-key entry surface
  (needs-design **#13**); there is no UI to supply a key, so a live or browser-served run can't be
  exercised yet. Resumes when #13 is designed.
- **Undesigned surfaces** — error/cancelled message rendering (#14), diff reject control + status
  copy (#15), dark palette (#16), RTL + direction override (#17), sidebar layout (#18). Their
  headless logic is built + tested; only the rendering waits on committed designs (rule 51).
- **Live-API runs** — never in `pnpm check:all` (rule 65 §8).
