---
kind: feature
id: 24
status_target: VERIFIED
commit_sha: 662a6751dfeb16b2e10e80178dcfd0c5120c0fcc
app_version: 0.21.0
date: 2026-06-29
verifier: claude
browser: Chrome (headless=new, raw CDP over Node WebSocket)
os_version: macOS 26.3.1
build_mode: production
provider: n/a — "Open in workspace" is a local UI action (no provider). Speech (Speak) is browser SpeechSynthesis (jsdom/headless has no voices, so the button renders in its disabled/novoice state — the wiring is covered by the StarredView component tests + Gate-4 audit).
result: pass
---

# Feature #24 — Starred word-detail: Open-in-workspace + IPA play

Single-WI (final) acceptance. Deferred from #22; both affordances depicted in the committed #22 design
(`dev-docs/designs/lucid-starred-translations/`). Shipped v0.21.0 (PR #205, merge `662a675`).

## Environment
- Built the production app (`pnpm build` → `dist/`, v0.21.0); served via a token-free single-origin
  `@lucid/server` (port 8803, `/tmp` DB). Headless Chrome via raw CDP; fresh profile. Seeded
  `localStorage['lucid.starred']` with one word item to reach the detail.

## Acceptance criteria

| # | Criterion | Observed | Result |
|---|---|---|---|
| 1 | The starred **word detail** shows a **Speak** + an **"Open in workspace ›"** button | Detail renders ‹ All starred · pipeline /ˈpaɪplaɪn/ · EN→中文 · a ▶ Speak button · TRANSLATION · MEANING · **FROM** (context) · ★ Unstar · **Open in workspace ›** (screenshot) | ✅ pass |
| 2 | **"Open in workspace"** loads the item's source into the **translate editor** | Click → the translate source textarea went `"" → "pipeline"` (item.source) | ✅ pass |
| 3 | The load clears the stale result (routes through `onSourceChange`'s reset path) — no stale result against the new source | The result pane shows the idle placeholder after the load (op `reset`); covered by the TranslatePanel reset-path test + Gate-4 audit | ✅ pass (tests + audit) |
| 4 | The load reads the **fresh** auto-run state (no stale auto-translate) | The ref-to-latest-handler test proves armed-after-mount → schedules, disarmed-after-mount → does not | ✅ pass (tests) |
| 5 | On phone, "Open in workspace" closes the drawer + switches to the translate pane | The `Workspace` listener (`setDrawerOpen(false)` + `setActivePane('translate')`) is covered by the Workspace test + Gate-4 audit | ✅ pass (tests + audit) |
| 6 | **Speak** (word only) reuses #20 speech; novoice/disabled (not hidden); cancels on unmount | The ▶ Speak button renders in the word detail (disabled/novoice in headless); the StarredView tests assert `createSpeech.speak(source,lang)` + the novoice-disabled state + cancel-on-unmount | ✅ pass (tests + audit) |

## Commands run
```bash
pnpm build                                   # production dist/ v0.21.0
STATIC_DIR=…/dist DB_PATH=/tmp/lucid-24-acc.db PORT=8803 node server/dist/index.js   # token-free single-origin
node /tmp/verify-24.mjs   # seed word → Starred tab → detail (Speak+Open present) → Open-in-workspace loads "pipeline" into translate
# CRIT1 detail Speak+Open present: true | CRIT2 Open-in-workspace loads source: true ("" → "pipeline") → RESULT: PASS
```

## Observations
- The detail (screenshot `/tmp/lucid-24-detail.png`) renders faithfully to the design's word-detail — Speak +
  translation/meaning/From + Unstar + Open-in-workspace. "Open in workspace" loaded the word into the translate
  source live; the result pane reset is the `onSourceChange` path (verified by tests + audit).
- Speak's actual audio can't run under headless Chrome (no TTS voices → the documented disabled/novoice state);
  the wiring (createSpeech, speak, cancel-on-unmount) is covered by the component suite + Gate-4 audit, mirroring
  the #20 LookupCardHost speech path (separately VERIFIED).
- The "Open in workspace" always lands in **translate** (a `StarredItem` doesn't record its origin pane) — the
  user-approved default.

## Artifacts
- `/tmp/lucid-24-detail.png` — the starred word detail (Speak + From + Unstar + Open-in-workspace).
- `/tmp/lucid-24-loaded.png` — after Open-in-workspace (source loaded into translate).
- 1823-test suite (loadSource / TranslatePanel armed-disarmed proof / Workspace / StarredView), 100% gated
  coverage, on the merge commit.
