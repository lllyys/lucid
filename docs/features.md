# Features

Feature tracker for lucid. Lifecycle: `TODO → PLANNED → IN PROGRESS → DONE → VERIFIED`
(see `.claude/rules/47-feature-workflow.md`). One row per feature.

| ID | Title | Status | Priority | Notes |
|----|-------|--------|----------|-------|
| 1 | Project scaffold — Vite + React 19 + TypeScript + Tailwind v4 + shadcn/ui + Zustand + Vitest, wired to a `pnpm check:all` gate | VERIFIED | High | The app foundation: build tooling + the LLM provider layer (Anthropic, behind the single interface) + config store + i18n, behind a minimal placeholder shell. All 7 WIs merged (v0.0.1 → v0.1.0). Plan: `dev-docs/plans/20260614-feature-1-project-scaffold.md` (Gate 2: 3 Codex rounds). Verified: `dev-docs/verification/feature-1-20260614.md`. GH: #1 |
| 2 | Lucid Workspace UI — translate + polish product surface (header + main-toolbar provider switcher, translate pane, polish pane with result/diff); no sidebar (→ #3) | VERIFIED | High | Shipped in v0.2.0 — all 9 WIs committed. Plan: `dev-docs/plans/20260614-feature-2-workspace-ui.md` (v5). Gate 2 closed via rule-47 accept (3 Codex rounds `019ec6a0` → `019ec6bb`). Mock-verified (rule 65 §8); 352 tests, 100% coverage. Verified: `dev-docs/verification/feature-2-20260615.md`. Undesigned surfaces → needs-design #13–#18; real-key e2e blocked on #13. Sidebar → #3. GH: #11 |
| 3 | Sessions & Glossary sidebar — data layer (sessions/tasks history, glossary store, persistence) split out of #2 | VERIFIED | Medium | Shipped in v0.4.0 — all 7 WIs merged (PR #24). Plan: `dev-docs/plans/20260615-feature-3-sidebar.md` (v2). Gate 2: independent subagent audit (Codex rate-limited; rule-48 fallback) — r1 NEEDS REVISION → v2, r2 READY TO BUILD. Gate 4: 4-subagent audit → `.claude/codex-audits/feat-feature-3-sidebar-audit.md` (3 High focus fixes + Mediums resolved; persistence/security CLEAN). 498 tests, 100% coverage. Verified: `dev-docs/verification/feature-3-20260615.md`. Full sidebar variant only (shell/hidden = prototype-only dock; product collapse is future needs-design). GH: #19 |
| 4 | Lucid Workspace — designed gap surfaces: provider Settings/API-key entry (#13), error/cancelled/partial banner (#14), diff Reject + per-hunk accept (#15), dark theme + token table (#16), RTL + direction override (#17) | VERIFIED | High | Shipped in v0.3.0 — all 7 WIs merged (PR #22). Plan: `dev-docs/plans/20260615-feature-4-gap-surfaces.md` (v4); Gate 2: 3 Codex rounds (`019ec907`→`019ec934`, v4 accepted at ceiling). Gate 4: 3 Codex rounds (`019ec974`→`019ec98c`) → `.claude/codex-audits/feat-feature-4-gap-surfaces-audit.md` (bidi converged on authoritative UCD data; dark-AA fixed). Mock-verified (rule 65 §8); 425 tests, 100% coverage. Verified: `dev-docs/verification/feature-4-20260615.md`. #13 lifts mock-only (real key via Settings). Designs: #13–#17. GH: #21 |
| 5 | Multi-provider support — implement OpenAI / Gemini / local·Ollama behind the existing `LLMProvider` interface | VERIFIED | High | Shipped v0.5.1→v0.6.0 (PRs #40–#43, #48, #49, #50). All 4 named providers + custom stream behind the single `LLMProvider` interface; per-vendor in-memory keys (rule 65 §5); redesigned #29 Settings (rail + key panels + model picker + test-connection + stat tiles + custom base-URL/optional-key + Ollama no-key + workspace-default). 8 WIs: registry/Gemini adapter/per-vendor store/factory (WI-1–4), test-connection probe (WI-5), Settings rebuild (WI-6a/6b), switcher + acceptance (WI-7). Plan: `dev-docs/plans/20260615-feature-5-multi-provider.md` (v3). Every WI Gate-4 CLEAN (`.claude/codex-audits/feat-feature-5-wi-*`); WI-5/6a/6b adversarial multi-lens workflows. 608 tests, 100% coverage. Verified: `dev-docs/verification/feature-5-20260616.md`. Folded in #6 + #7. GH: #27 |
| 6 | Verify API key / test connection — confirm an entered key actually authenticates | VERIFIED | Medium | Delivered within feature #5 (v0.5.5–v0.5.7): the headless `probeProvider` (WI-5, `src/lib/providers/testConnection.ts`) makes a minimal authenticated probe via the provider interface (no key logging, timeout-guarded, never in `check:all`); the Settings connection card + Test button + stat tiles (WI-6b, `useTestConnection`) report valid/invalid/unreachable inline with latency. Tests the viewed vendor (any of the 4 + custom). Gate-4: WI-5 + WI-6b adversarial 3-lens workflows, CLEAN. Verified: `dev-docs/verification/feature-6-20260616.md`. Triage 2026-06-15 ("can't add/test a key"). GH: #28 |
| 7 | Custom / OpenAI-compatible provider — user-defined base URL + model + optional key | VERIFIED | High | Complete. Engine landed v0.4.1 (PR #35): `openaiCompatibleStream` + `custom` vendor/registry (`allowAnyModel`) + factory/store. The design-gated Settings fields (WI-4) landed within feature #5's WI-6a (v0.5.6) after the #29 design loop: base-URL field + free-text model + an OPTIONAL key (user decision #5/#7/#29 — keyless self-hosted AND keyed proxies like OpenRouter), and `usePanelRun` threads `baseUrl` so custom actually runs. `isReady(custom)`=baseUrl+model; factory exempts custom from the key requirement; keyless omits the Bearer header. Gate-4: engine `.claude/codex-audits/feat-feature-7-custom-provider-audit.md` + WI-6a 4-lens workflow, CLEAN. Verified: `dev-docs/verification/feature-7-20260616.md`. Triage 2026-06-15 ("we need the custom provider"). GH: #31 |
| 8 | Persist polish keywords across reloads | VERIFIED | Medium | Shipped in v0.5.0 (PR #37, merge `79457a9`). `polishKeywordsStore` persists globally via the same crash-proof `safeJSONStorage` as sessions/glossary (`lucid.keywords`, version 1, migrate + partialize) — one keyword set across sessions (global chosen over per-session to avoid a cross-feature `sessionStore` import). Plan: `dev-docs/plans/20260615-feature-8-persist-keywords.md` (v2; Gate 2 READY TO BUILD after fixing a false localStorage-exists premise). Gate 4: `.claude/codex-audits/feat-feature-8-wi-1-persist-keywords-audit.md` (independent subagent, CLEAN/ship-as-is). 528 tests, 100% coverage. Verified: `dev-docs/verification/feature-8-20260615.md` (compositional persistence verification; live-browser reload deferred per #2/#3/#4 precedent). Triage 2026-06-15 ("persist … key words too"). GH: #33 |
| 9 | Self-hosted server-side persistence + sync — sessions, history, glossary & keywords | PLANNED | Medium | **NET-NEW (lucid's first backend).** User chose a SELF-HOSTED server (not cloud). Plan: `dev-docs/plans/20260616-feature-9-self-hosted-sync.md` (Gate-1 + Gate-2 CLEAN). Stack: a thin **Hono + SQLite** server in a separate `server/` package; browser talks via one injectable `SyncBackend` (provider-layer pattern); pure headless merge/conflict logic. **Server-`rev` is the ordering authority** (clock-skew-proof — Phase-0 spike `dev-docs/grills/feature-9-sync/spike.mjs` PASSES 6/6). Sync is **opt-in; local-only stays first-class** (Ollama + self-host = zero-third-party). Decisions: ship v1 per-entity LWW + surfaced conflict signal (review UI deferred); persist the sync token (sensitive, TLS-only); keys stay client-side. Cross-model: Codex Gate-2 READY-TO-BUILD + a 6-agent design workflow (option panel→synthesis→adversarial review). 10 WIs (WI-0–8 headless/server buildable; **WI-9 account/sync UI is design-gated, rule 51 → needs-design**). Distinct from #3/#8 (localStorage). GH: #45 |
