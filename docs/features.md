# Features

Feature tracker for lucid. Lifecycle: `TODO → PLANNED → IN PROGRESS → DONE → VERIFIED`
(see `.claude/rules/47-feature-workflow.md`). One row per feature.

| ID | Title | Status | Priority | Notes |
|----|-------|--------|----------|-------|
| 1 | Project scaffold — Vite + React 19 + TypeScript + Tailwind v4 + shadcn/ui + Zustand + Vitest, wired to a `pnpm check:all` gate | VERIFIED | High | The app foundation: build tooling + the LLM provider layer (Anthropic, behind the single interface) + config store + i18n, behind a minimal placeholder shell. All 7 WIs merged (v0.0.1 → v0.1.0). Plan: `dev-docs/plans/20260614-feature-1-project-scaffold.md` (Gate 2: 3 Codex rounds). Verified: `dev-docs/verification/feature-1-20260614.md`. GH: #1 |
| 2 | Lucid Workspace UI — translate + polish product surface (header + main-toolbar provider switcher, translate pane, polish pane with result/diff); no sidebar (→ #3) | VERIFIED | High | Shipped in v0.2.0 — all 9 WIs committed. Plan: `dev-docs/plans/20260614-feature-2-workspace-ui.md` (v5). Gate 2 closed via rule-47 accept (3 Codex rounds `019ec6a0` → `019ec6bb`). Mock-verified (rule 65 §8); 352 tests, 100% coverage. Verified: `dev-docs/verification/feature-2-20260615.md`. Undesigned surfaces → needs-design #13–#18; real-key e2e blocked on #13. Sidebar → #3. GH: #11 |
| 3 | Sessions & Glossary sidebar — data layer (sessions/tasks history, glossary store, persistence) split out of #2 | VERIFIED | Medium | Shipped in v0.4.0 — all 7 WIs merged (PR #24). Plan: `dev-docs/plans/20260615-feature-3-sidebar.md` (v2). Gate 2: independent subagent audit (Codex rate-limited; rule-48 fallback) — r1 NEEDS REVISION → v2, r2 READY TO BUILD. Gate 4: 4-subagent audit → `.claude/codex-audits/feat-feature-3-sidebar-audit.md` (3 High focus fixes + Mediums resolved; persistence/security CLEAN). 498 tests, 100% coverage. Verified: `dev-docs/verification/feature-3-20260615.md`. Full sidebar variant only (shell/hidden = prototype-only dock; product collapse is future needs-design). GH: #19 |
| 4 | Lucid Workspace — designed gap surfaces: provider Settings/API-key entry (#13), error/cancelled/partial banner (#14), diff Reject + per-hunk accept (#15), dark theme + token table (#16), RTL + direction override (#17) | VERIFIED | High | Shipped in v0.3.0 — all 7 WIs merged (PR #22). Plan: `dev-docs/plans/20260615-feature-4-gap-surfaces.md` (v4); Gate 2: 3 Codex rounds (`019ec907`→`019ec934`, v4 accepted at ceiling). Gate 4: 3 Codex rounds (`019ec974`→`019ec98c`) → `.claude/codex-audits/feat-feature-4-gap-surfaces-audit.md` (bidi converged on authoritative UCD data; dark-AA fixed). Mock-verified (rule 65 §8); 425 tests, 100% coverage. Verified: `dev-docs/verification/feature-4-20260615.md`. #13 lifts mock-only (real key via Settings). Designs: #13–#17. GH: #21 |
| 5 | Multi-provider support — implement OpenAI / Gemini / local·Ollama behind the existing `LLMProvider` interface | VERIFIED | High | Shipped v0.5.1→v0.6.0 (PRs #40–#43, #48, #49, #50). All 4 named providers + custom stream behind the single `LLMProvider` interface; per-vendor in-memory keys (rule 65 §5); redesigned #29 Settings (rail + key panels + model picker + test-connection + stat tiles + custom base-URL/optional-key + Ollama no-key + workspace-default). 8 WIs: registry/Gemini adapter/per-vendor store/factory (WI-1–4), test-connection probe (WI-5), Settings rebuild (WI-6a/6b), switcher + acceptance (WI-7). Plan: `dev-docs/plans/20260615-feature-5-multi-provider.md` (v3). Every WI Gate-4 CLEAN (`.claude/codex-audits/feat-feature-5-wi-*`); WI-5/6a/6b adversarial multi-lens workflows. 608 tests, 100% coverage. Verified: `dev-docs/verification/feature-5-20260616.md`. Folded in #6 + #7. GH: #27 |
| 6 | Verify API key / test connection — confirm an entered key actually authenticates | VERIFIED | Medium | Delivered within feature #5 (v0.5.5–v0.5.7): the headless `probeProvider` (WI-5, `src/lib/providers/testConnection.ts`) makes a minimal authenticated probe via the provider interface (no key logging, timeout-guarded, never in `check:all`); the Settings connection card + Test button + stat tiles (WI-6b, `useTestConnection`) report valid/invalid/unreachable inline with latency. Tests the viewed vendor (any of the 4 + custom). Gate-4: WI-5 + WI-6b adversarial 3-lens workflows, CLEAN. Verified: `dev-docs/verification/feature-6-20260616.md`. Triage 2026-06-15 ("can't add/test a key"). GH: #28 |
| 7 | Custom / OpenAI-compatible provider — user-defined base URL + model + optional key | VERIFIED | High | Complete. Engine landed v0.4.1 (PR #35): `openaiCompatibleStream` + `custom` vendor/registry (`allowAnyModel`) + factory/store. The design-gated Settings fields (WI-4) landed within feature #5's WI-6a (v0.5.6) after the #29 design loop: base-URL field + free-text model + an OPTIONAL key (user decision #5/#7/#29 — keyless self-hosted AND keyed proxies like OpenRouter), and `usePanelRun` threads `baseUrl` so custom actually runs. `isReady(custom)`=baseUrl+model; factory exempts custom from the key requirement; keyless omits the Bearer header. Gate-4: engine `.claude/codex-audits/feat-feature-7-custom-provider-audit.md` + WI-6a 4-lens workflow, CLEAN. Verified: `dev-docs/verification/feature-7-20260616.md`. Triage 2026-06-15 ("we need the custom provider"). GH: #31 |
| 8 | Persist polish keywords across reloads | VERIFIED | Medium | Shipped in v0.5.0 (PR #37, merge `79457a9`). `polishKeywordsStore` persists globally via the same crash-proof `safeJSONStorage` as sessions/glossary (`lucid.keywords`, version 1, migrate + partialize) — one keyword set across sessions (global chosen over per-session to avoid a cross-feature `sessionStore` import). Plan: `dev-docs/plans/20260615-feature-8-persist-keywords.md` (v2; Gate 2 READY TO BUILD after fixing a false localStorage-exists premise). Gate 4: `.claude/codex-audits/feat-feature-8-wi-1-persist-keywords-audit.md` (independent subagent, CLEAN/ship-as-is). 528 tests, 100% coverage. Verified: `dev-docs/verification/feature-8-20260615.md` (compositional persistence verification; live-browser reload deferred per #2/#3/#4 precedent). Triage 2026-06-15 ("persist … key words too"). GH: #33 |
| 9 | Self-hosted server-side persistence + sync — sessions, history, glossary & keywords | VERIFIED | Medium | **WI-0 done (v0.6.1):** extracted shared async primitives (realSleep/expBackoff/clampMs) to `src/lib/async/backoff.ts` (sync reuses them without cross-importing the provider layer); Gate-4 CLEAN. **WI-1a done (v0.6.2, PR #56):** sessionStore sync envelope (`updatedAt`/`deletedAt`) + PERSIST_VERSION 1→2 never-throwing backfill migration; Gate-4 CLEAN (3-round Codex gpt-5.5, ship-as-is); surfaced pre-existing id-collision bug #55. **WI-1b done (v0.6.3, PR #57):** glossaryStore sync envelope (Term gains `createdAt`/`updatedAt`/`deletedAt`; v1 backfill = `0` legacy sentinel) + **hardened BOTH store migrations** to skip malformed-but-object entries (a non-string label/name would crash `addTerm`/`searchSessions` `.toLowerCase()`) — Gate-4 CLEAN (3-round Codex, ship-as-is). **WI-1c done (v0.6.4, PR #58) — WI-1 COMPLETE:** keyword `string[]`→`Keyword[]{id,value,updatedAt,deletedAt}`; `id` is a **collision-free** deterministic encoding of the value (fixed-width UTF-16 hex — cross-device convergence; a hash collided + `encodeURIComponent` threw on lone surrogates, both caught by audit); migration trims/dedupes; PolishPanel maps values at the boundary (prompt layer + KeywordsCard unchanged). Gate-4 CLEAN (3-round Codex, ship-as-is). **WI-2 done (v0.6.5, PR #59):** `src/lib/sync/{types,guards}.ts` — SyncEntity/EntityType/PushOp/results/SyncError + hand-written boundary guards (no zod) with safe-integer rev/cursor + non-array-payload + conflict-id-match validation; extracted shared `isRecord`→`src/lib/guards.ts` (3 stores de-duped); tdd-guard scope += `src/lib/sync/**`. Gate-4 CLEAN (3-round Codex). **WI-3 done (v0.6.6, PR #61):** pure `src/lib/sync/merge.ts` `mergeEntities(local,remote,pending)`→`{resolved,conflicts}` — server-rev-primary LWW + delete-wins (rev-only authority; clock-skew immune), remote normalized by highest-rev (dup-id supersession-masking caught by audit); +`Conflict`/`MergeResult` types. Gate-4 CLEAN (2-round Codex). **WI-4 done (v0.6.7, PR #62):** `src/lib/sync/backend.ts` — `SyncBackend` {pull/push/purge} + `createRestSyncBackend({baseUrl,token,fetch?,timeoutMs?})`; bearer auth, AbortController timeout (covers headers+body), guard-validated responses, `SyncError` mapping, never-throws `BackendResult`; push correlates results to op ids. Gate-4 CLEAN (2-round Codex — caught a stringify-throw, unbounded-body, header-shadow, push-id-mismatch). Retry/queue deferred to WI-6. **WI-5a done (v0.6.8, PR #63):** pure `src/lib/sync/seed.ts` `buildSeedFromLocal(snapshot)→PushOp[]` — flattens sessions into session + per-task ops (keyed by `sessionId`) + term/keyword ops, all `baseRev:0` (idempotent re-seed); Gate-4 CLEAN (1 round). Seed-execution loop + `disconnectSync` + purge + seeded-flag deferred to WI-7. **WI-6a done (v0.6.9, PR #64):** pure `src/lib/sync/queue.ts` offline push-queue — dedup-by-id collapse + seq-based `ack` (mid-flight edit never lost, verified under single-in-flight); Gate-4 CLEAN (1 round). Drain loop/debounce/listeners/inert-when-off deferred to WI-7. **WI-7a done (v0.6.10, PR #65):** `src/stores/syncStore.ts` — config/state seam (8-state status machine matching the design; config/cursor/seeded persisted via a sanitizing migrate; counts/conflict transient). **Sync token PERSISTED** as a documented rule-65-§5 exception (reload-surviving background sync, TLS-only, never logged). Gate-4 CLEAN (2-round). WI-7b (async engine) sliced bottom-up. **WI-7b-i done (v0.6.11, PR #66):** pure `collectLocal` merge input (shares `flattenLocal` with the seed). **WI-7b-ii done (v0.6.13, PR #68):** validated sync→domain reconstructors (`src/lib/sync/reconstruct.ts`; safe-int timestamps + keyword id-consistency); extracted shared `isNonNegInt` + pure `keywordId` module (reconstruct stays side-effect-free); Gate-4 CLEAN (3 rounds). **WI-7b-iii done (v0.6.14, PR #69):** pure `reconcileStores` two-pass apply (re-nest/upsert/delete-wins-by-id); Gate-4 CLEAN. **WI-7b-iv done (v0.6.15, PR #70):** pure-async `syncPull` (pull→merge→reconcile→monotonic cursor; pending revs from the queue, no separate rev map); Gate-4 CLEAN (3 rounds). **WI-7b-v done (v0.6.16, PR #71):** pure-async `syncPush` — per-entry `pushed[]` (applied+rev / conflict tied to its entry); Gate-4 CLEAN (2 rounds). Remaining WI-7b: the engine (WI-7b-vi) — persisted per-id rev map, revise syncPull to return resolved revs, ack-gated apply, status + lifecycle. **WI-9 DESIGN LANDED & UNBLOCKED:** `dev-docs/designs/lucid-sync/` (PR #60, #53 closed) — the #29-style claude.ai/design loop; covers all 8 sync states + connect/connected/conflict/disconnect/error surfaces. **NET-NEW (lucid's first backend).** User chose a SELF-HOSTED server (not cloud). Plan: `dev-docs/plans/20260616-feature-9-self-hosted-sync.md` (Gate-1 + Gate-2 CLEAN). Stack: a thin **Hono + SQLite** server in a separate `server/` package; browser talks via one injectable `SyncBackend` (provider-layer pattern); pure headless merge/conflict logic. **Server-`rev` is the ordering authority** (clock-skew-proof — Phase-0 spike `dev-docs/grills/feature-9-sync/spike.mjs` PASSES 6/6). Sync is **opt-in; local-only stays first-class** (Ollama + self-host = zero-third-party). Decisions: ship v1 per-entity LWW + surfaced conflict signal (review UI deferred); persist the sync token (sensitive, TLS-only); keys stay client-side. Cross-model: Codex Gate-2 READY-TO-BUILD + a 6-agent design workflow (option panel→synthesis→adversarial review). 10 WIs (WI-0–8 headless/server buildable; **WI-9 account/sync UI is design-gated, rule 51 → needs-design**). **COMPLETE + VERIFIED (v0.7.0, 2026-06-18):** the remaining WIs all merged — WI-7b-vi/d (the headless orchestrator: persisted rev map, runCycle engine, offline queue, diff/editTracker echo-guard, orchestrator lifecycle, createSyncController) v0.6.17–0.6.25 PRs #72–#80; **WI-8** the self-hosted server (`server/` @lucid/server — Hono + node:sqlite, atomic-rev+conflict store, bearer-auth HTTP layer, serve entry + Docker + deploy doc) v0.6.26–0.6.29 PRs #81–#84; **WI-9** the sync UI (a status pill, b controller actions syncNow/disconnect-erase, c the Settings·Sync panel, d mount + error banners + wiring resume-on-mount) v0.6.30–0.7.0 PRs #85–#88. Gate-5 acceptance PASS — real client↔server round-trip (16/16 assertions: auth, monotonic-rev apply, pull round-trip, server-wins conflict, restful update, incremental pull, purge, unreachable) + persistence-across-restart + headless-Chromium render of the mounted pill + the 1003-test suite at 100% gated coverage. Evidence: `dev-docs/verification/feature-9-2026-06-18.md`. Distinct from #3/#8 (localStorage). GH: #45 |
| 10 | Multiple custom (OpenAI-compatible) providers — configure & switch between several user-defined endpoints, each with its own base URL / model / optional key | VERIFIED | Medium | Extends #7 (single custom provider) from one → N. Today `Vendor` is a fixed enum with one `custom` slot + a single `baseUrl` (`src/stores/providerStore.ts`); supporting multiple needs a dynamic provider identity — a `customProviders` collection keyed by id (`{id,label,baseUrl,model,key}`), active-by-id — rippling through providerStore (per-id keys/models/testResults vs the single `custom` slot), the factory/registry (`modelRegistry`/`isVendorImplemented`/`resolveModel`/`providers/index.ts`), `providerPresentation`/`configurablePresentations`, `ProviderSwitcher`, and persistence. The `openaiCompatibleProvider` engine is already reusable per base-URL/model — this is mostly a one→many data-model + UI change. Per-custom in-memory keys (rule 65 §5). NEW Settings UI (rail list of customs + add/remove + per-custom config) is **rule-51 design-gated → needs-design bundle before Gate-3**. Route via `/feature-workflow` (plan → Gate-2 audit → TDD → design-gated UI → verify). Triage 2026-06-18 ("we need multi custom provider"). GH: #92 · Design: `dev-docs/designs/lucid-custom-providers/` (PR #122; #109 closed). Plan: `dev-docs/plans/20260619-feature-10-custom-providers.md` (Gate-2 audited r1 → 3C/4H/4M → v2 resolved; 0 open). 4 WIs: WI-1 store one→many+migration, WI-2 presentation+call-site resolution, WI-3 rail UI, WI-4 switcher. |
| 11 | Auto-run translate & polish after the user finishes entering text (debounced auto-trigger, not just the manual Translate/Polish buttons) | VERIFIED | Medium | Runs are manual today (`TranslatePanel`/`PolishPanel` buttons → `usePanelRun().run`). Want a debounced auto-run on settle, reusing the existing run pipeline (`usePanelRun` `run`/`abort` + the AbortController for abort-on-new-input). Key considerations: **API cost** (debounce + likely an opt-in toggle + min-length guard so hosted providers don't fire surprise paid calls); **CJK/IME composition** (don't fire mid-compose — app is CJK-first); **provider-not-ready** guard (`isReady`); empty/whitespace/paste/rapid-edit; clean stream-abort on re-edit. Any NEW control (auto-run toggle / indicator) is **rule-51 design-gated**. Route via `/feature-workflow`. Triage 2026-06-18 ("we need auto translation and polishing after entering"). GH: #94 · Design: `dev-docs/designs/lucid-auto-run/` (PR #122; #108 closed). Plan: `dev-docs/plans/20260619-feature-11-auto-run.md` (Gate-2 audited r1 → 3C/3H/4M → v2 resolved; 0 open). 2 WIs: WI-1 useAutoRunDebounce hook + isAuto plumbing, WI-2 toggle/pending/AUTO/cost-gate/IME UI. |
| 12 | Persist provider configuration (active vendor, per-vendor model, custom base URL) across reloads — API keys stay in-memory (rule 65 §5) | VERIFIED | Medium | **Shipped v0.8.0 (PR #103, merge `1a1547f`), VERIFIED 2026-06-18.** WI-1 wraps `providerStore` in `persist` — `partializeProvider` allowlist `{vendor,models,baseUrl}` (keys structurally off-disk, §5), `mergeProvider` (`{...current,…}` preserves actions; membership-guards vendor — caught the planned `isVendorImplemented` throw; prototype-pollution-safe overlay; re-derives the model mirror), `migrateProvider` + `createSafeJSONStorage`. Gate-2 plan audit (revised clean) + Gate-4 ship-as-is (independent Claude auditor; Codex quota-blocked). Gate-5b browser-verified: persisted blob excludes the key + a seeded config rehydrates the switcher to Custom (`dev-docs/verification/feature-12-20260618.md`). 17 persist tests, 100% gated coverage. `src/stores/providerStore.ts` was the ONLY store with **no `persist` middleware** — every field is in-memory, so a reload resets `vendor`→anthropic, `baseUrl`→'', `models`→defaults. Keys-in-memory is by design (§5); non-secret config persistence was never built. **NOT related to sync** (#9 persists workspace DATA — sessions/glossary/keywords — to the server, not provider config; `providerStore` is client-side + unsynced). Fix: wrap `providerStore` in `persist` + `createSafeJSONStorage` with a `partialize` persisting ONLY `{vendor, models, baseUrl}` — **NEVER `apiKeys`/`apiKey`/`testResults`** (load-bearing §5 constraint); sanitizing `migrate` + an unimplemented-vendor guard; re-derive mirrors on rehydrate. Reuses the session/glossary/sync persist pattern. Relates to #10. Route via `/feature-workflow`. Triage 2026-06-18 ("the custom provider can't persist"). GH: #95 |
| 13 | Auto-expanding text editors (draft / original / source) — grow to content instead of a fixed-height inner scroll | VERIFIED | Medium | The 3 editable textareas were `min-h-0 flex-1 resize-none` (fixed flex-share → inner scroll); now `field-sizing-content min-h-[88px] max-h-[88vh]` (grow-to-content + 88vh cap). Polish cards → `shrink-0` (content-sized; the `overflow-auto` column scrolls, Keywords reachable); TranslatePanel section → `shrink-0` (grows with the Source; `<main>` scrolls) + row → `items-start`. Scope: editable inputs only (result panes + Keywords excluded). Design delivered: `dev-docs/designs/lucid-auto-expanding-editors/` (PR #116; design #107 closed). Plan: `dev-docs/plans/20260619-feature-13-auto-expanding-editors.md` (Gate-2 audited 1 round → v2; Gate-4 ship-as-is; Gate-5 browser-verified via CDP — grow/cap/no-overlap/column-reflow). Triage 2026-06-18 ("we don't need the scroll bar; can this box auto-expand?"). GH: #97. |
| 14 | Auto-save every completed translate/polish run to session history (no Accept required) | VERIFIED | Medium | **Shipped v0.9.0 (PR #105, merge `2888578`), VERIFIED 2026-06-18.** Every completed run auto-records a session task (`src/lib/sessions/autoRecord.ts` `recordRunIfNew` — module-scoped `Map<PanelId,number>` dedup, remount-safe; `useAutoRecordTask` hook in both panels; polish stores the cleaned result via #96); `recordTask` removed from both accept handlers (Accept → commit-to-editor only). Gate-2 audit (r1→r2 READY) + Gate-4 follow-up-recommended (0 Critical/High/Medium). Gate-5b verification-exception (run needs a provider/key unavailable to a headless browser; rule 65 §8): real-component+store integration tests, 32 pass — `dev-docs/verification/feature-14-20260618.md`. **#11 must add debounce/dedupe before it ships** (auto-run + auto-save = spam). Plan: `dev-docs/plans/20260618-feature-14-autosave-runs.md`. Today history saves only on Accept (`recordTask` in `TranslatePanel.tsx:59` / `PolishPanel.tsx:106` accept handlers) → un-Accepted runs never appear (sidebar "Nothing saved yet"), contradicting the empty-state copy "appear here as you translate and polish". Want: record on operation `done` (operationStore/usePanelRun), completed runs only (not error/cancelled); Accept narrows to "commit to editor". Reuses `recordTask` (auto-creates a session). Considerations: dedupe re-runs (avoid history spam); MUST be co-designed with **#11** (auto-run) to avoid per-keystroke spam; **#96** (polish prose) should land first so the saved resultText is clean; the empty-state copy becomes accurate. Route via `/feature-workflow`. Resolves the 2026-06-18 NEEDS-INFO ("no sessions/history appears after translate/polish"). GH: #98 |
| 15 | Cross-device E2E-encrypted provider config + API key via the self-hosted server (no token, single-origin) | VERIFIED | High | Triage 2026-06-19: user wants to open the app on any device (over Tailscale) and work WITHOUT re-entering the API key AND without a sync URL/token. localStorage is per-device; the sync token is per-device → cross-device + no-token requires the config+key to live on the single self-hosted server the user reaches, served same-origin. Decision: **end-to-end encryption** — the server stores only ciphertext; a passphrase per device decrypts (server never sees the key; stronger than plaintext-on-server). Scope: native WebCrypto (no deps) PBKDF2→AES-GCM crypto module; a single-user opaque encrypted-config endpoint on `@lucid/server` + serve the app bundle (single-origin → no CORS/no URL to type); client fetch→passphrase→decrypt→hydrate `providerStore`; re-encrypt+PUT on change. The **passphrase-entry UI is rule-51 design-gated** (needs-design before that WI) — crypto/server/client logic buildable headless first (rule 51 logic-first). Supersedes #12's keys-in-memory (documented §5 exception). Plan: `dev-docs/plans/20260619-feature-15-e2e-config-sync.md` (Gate-2 audited: r1 NEEDS REVISION (incl. the disqualifying C1 — browser crypto needs HTTPS) → v2 → r2 0 Crit/High, 3 Med (M1/LWW) → v3 resolved by dropping the client clock; 0 open). HTTPS precondition accepted (one-time `tailscale serve`). WI-1–5 SHIPPED (crypto v0.9.1, codec v0.9.2, server /config v0.9.3, serve-app v0.9.4, client configSync v0.10.1). WI-6 passphrase/unlock UI + WI-7 controller/wiring SHIPPED v0.13.0 (design #120 landed → `dev-docs/designs/lucid-passphrase-unlock/`; custom-provider keys now sync encrypted; server holds ciphertext only — real-server browser E2E verified). GH: #111 · #120. |
| 16 | Responsive / mobile layout — workspace adapts to phone / narrow viewports (sidebar, translate/polish panes, chrome) | VERIFIED | Medium | **Shipped v0.15.0 (PR #157, merge `a7f9c3a`), VERIFIED 2026-06-22.** Triage 2026-06-20 ("the interface didn't suit the phone"): desktop layout overflowed narrow viewports. Design landed via DesignSync MCP (`dev-docs/designs/lucid-responsive-mobile-layout`; resolves #140). 3 tiers (desktop ≥960 unchanged · tablet 600–959 drawer+stacked · phone <600 drawer+pane-switcher). 4 WIs: `useViewportTier` hook; shadcn-Sheet drawer; pane-switcher (both panels kept MOUNTED, visibility toggle — avoids state-loss) + panel stacking + PolishResult sticky; chrome reflow. **Resolves #17** (app-shell inner-scroll kept; `<main>` single scroll region; PolishPanel nested scroll dropped on phone). Gate-2 PASSED (4-lens Workflow, 2 rounds: r1 1 Crit + 7 High + 7 Med → r2 0 open) + Gate-4 ship-as-is + Gate-5 CDP-verified @ 1200/800/390 (no horizontal overflow at any tier; drawer + switcher + 44px chips; desktop unchanged; 0 console errors). 1467 tests, 100% gated coverage. Evidence: `dev-docs/verification/feature-16-20260622.md`. GH: #138 · #143 (#17). |
| 17 | Scroll the whole interface, not inner panes — workspace scroll model (page-scroll vs app-shell inner-scroll) | VERIFIED | Medium | **Resolved by feature #16, VERIFIED 2026-06-22 (v0.15.0, merge `a7f9c3a`).** Triage 2026-06-20 ("the scroll bar should not be within the area"): the inner-pane scrollbar bothered the user. The committed responsive design (user-produced via claude.ai/design) **kept the app-shell + inner-scroll model** rather than switch to whole-page scroll — a pinned footer/toolbar (Run stays thumb-reachable) is incompatible with `body` scroll. The complaint is resolved by making **`<main>` the single scroll region** (chrome pinned) and **dropping PolishPanel's nested input-column scroll on phone** (`min-[600px]:overflow-auto`), so content scrolls as ONE column, not per-card. Reconciled with #13 (`field-sizing-content` + tier-scoped cap 50vh phone / 88vh desktop). Decided in `dev-docs/plans/20260622-feature-16-responsive-layout.md` (#17 scroll-model section); verified with #16 (CDP no-overflow + no-nested-scroll). A "resolved by design decision" outcome (literal page-scroll not built; the user's own design chose app-shell). Evidence: `dev-docs/verification/feature-17-20260622.md`. GH: #143 · relates #138 (#16) · #97 (#13). |
| 18 | Polish-goal selector — choose clarity / tone / grammar / concise (currently hardcoded to 'clarity') | VERIFIED | Medium | **Shipped v0.14.0 (PR #149, merge `add7afac`), VERIFIED 2026-06-20.** Triage 2026-06-20 ("there should be clear buttons for polishing"): the polish goal was **hardcoded** to `'clarity'` (`PolishPanel.tsx`); the `lucid-workspace` bundle depicted a goal-chip row never built. WI-1 (behavioral/final): `GoalChips` single-select radiogroup (Clarity/Grammar/Tone/Concise, design order) in the polish header + `buildPolishRequest` goal-override + `onChangeGoal` (resetPolish + armPolish with the fresh goal) + i18n `polish.goal.*`. **Decision: single-select v1** (accepted design deviation — design is multi-select but goal instructions conflict when combined; multi-goal deferred). Plan `dev-docs/plans/20260620-feature-18-polish-goal-selector.md` (v3) — Gate-2 PASSED (independent Claude auditor, 2 rounds) + Gate-4 ship-as-is + Gate-5 CDP-verified (radiogroup renders, Clarity default, single-select, 0 console errors). 1383 tests, 100% gated coverage. Evidence: `dev-docs/verification/feature-18-20260620.md`. GH: #145. |
| 19 | Unify workspace-data sync (#9) onto the single-origin server — drop the separate token/URL config | VERIFIED | Medium | **Shipped + VERIFIED 2026-06-22 (v0.16.0).** Triage 2026-06-20 ("is the sync configuration still needed?" → user chose **unify onto single-origin**): the manual token+URL config was friction now the app runs single-origin. Decision: token-free `/sync` ONLY when `staticDir` set AND no token (origin + Tailscale ACL boundary, like `/config`). **WI-1+WI-2** (server token-free single-origin mode + client `connectSingleOrigin`) SHIPPED v0.14.1 (PR #153, `f7fe2153`). **WI-3** (simplified Settings·Sync UI — on/off single-origin toggle, Advanced disclosure hosting the existing remote URL+token form, token-aware connected row) SHIPPED v0.16.0 (PR #160, merge `ddfc446`); design via DesignSync MCP (`dev-docs/designs/lucid-settings-sync-simplified`, closes #151). Gate-2 PASSED (2 security rounds) + Gate-4 ship-as-is + Gate-5 CDP (toggle off→on→`connectSingleOrigin`→connected single-origin no-Edit; Advanced reveals remote form; 0 console errors). Backward-compat: existing remote token users keep server row + Edit (test-guarded). `error.syncSession` banner deferred (rule 51 — no invented auth flow). 1483 tests, 100% gated coverage. Evidence: `dev-docs/verification/feature-19-20260622.md`. Relates to #111 · #45. GH: #147. |
| 20 | Click-a-word dictionary — in-context translation, pronunciation (IPA) + meaning, with audio playback | VERIFIED | Medium | **Shipped + VERIFIED 2026-06-26 (v0.17.0, PR #171, merge `52c3aad`).** Click a word in a rendered result pane (`TranslateResult` + `PolishResult` Result view) → a popover shows {translation, IPA, in-context meaning} + a play button (browser `SpeechSynthesis`). Lookup runs through the provider layer (new `kind:'define'` + versioned JSON prompt; rule 65). Design via DesignSync MCP (`dev-docs/designs/lucid-word-lookup`; closed #166). 7 WIs (define-prompt · segmentation · parseDefine · speak wrapper · lookupStore+useWordLookup · ClickableText · popover). Gate-2 PASSED (4-lens, 2 rounds: r1 0 Crit + 4 High + 8 Med → r2 0 open) + Gate-4 ship-as-is + Gate-5 (high-fidelity integration/component/hook suite at real boundaries: click→popover→6 states→play/cancel→error→openSettings; runId stale-guard; done-unparseable→error; CJK/RTL; voice-race; provider mocked per §8 + CDP smoke clean — full real-Chrome provider-driven click blocked by headless auto-run automation friction, not a code gap). 1608 tests, 100% gated coverage. Evidence: `dev-docs/verification/feature-20-20260626.md`. v1 = 2 rendered panes; editable Original/Draft → #169. GH: #164 · #166 (design, closed) · #169 (follow-up). |
| 21 | Workspace sync on by default for a single-origin self-hosted server (sessions live on the server) | VERIFIED | Medium | **Shipped + VERIFIED 2026-06-28 (v0.18.0, PR #188, merge `03aadfa`).** Auto-connect/default-on ONLY for **token-free single-origin** (the #19 model — user's own box); tokened (probe→auth error→ineligible) + remote stay opt-in; one-time consent (no silent exfiltration, rule 65 §6). Gate-2 PASSED (2 rounds). **WI-1/WI-2 (eligibility probe `backend.pull(0)`+`isPullResult`; `autoSyncPrompt`/`showAutoPrompt` state + `maybeAutoConnect`/accept/decline + post-await race re-check) v0.17.1; WI-3 (consent prompt UI + Workspace load-wiring + AbortSignal) v0.18.0** — design `dev-docs/designs/lucid-sync-consent/` (DesignSync import; closed #177). All Gate-4 ship-as-is. **Gate-5 acceptance PASS** — real token-free single-origin `@lucid/server` (port 8799, no `SYNC_TOKEN`) + headless-Chrome CDP: probe→eligible→consent auto-appears (after the #15 config gate), scope list + API-keys-never + decline-focus, accept→connects+dismisses, reload→no re-prompt. 1693 tests, 100% gated coverage. Evidence: `dev-docs/verification/feature-21-20260628.md`. Route via `/feature-workflow`. Relates #9 · #19 · #15. GH: #175 · #177 (consent design, closed). |
| 22 | Star word/sentence translations + a review surface (saved-translations study list) | VERIFIED | Medium | **Shipped + VERIFIED 2026-06-29 (v0.20.0, PR #201, merge `5c2d08e`).** Net-new; **distinct from Glossary #3** (domain terms injected into prompts; this = a personal REVIEW list). Gate-2 PASSED (2 rounds; r1 caught a Critical — the server `VALID_TYPES` would reject a new entity + wedge ALL sync). **WI-1/WI-2 (headless `starredStore` uuid+content-scan+hard-delete + the `'starred'` #9 sync entity end-to-end incl. the self-hosted server) v0.17.2; WI-3 (star indicators — `StarButton` tuple-reflected on the #20 lookup popover [word] + translate/polish result toolbars [sentence]) + WI-4 (the "★ Starred" sidebar tab — list/search-both-halves/detail/empty/no-results + Unstar) v0.20.0** — design `dev-docs/designs/lucid-starred-translations/` (DesignSync import; closed #183). All Gate-4 ship-as-is. **Gate-5 acceptance PASS** — token-free single-origin `@lucid/server` + headless-Chrome CDP: Starred tab lists seeded word+sentence (type badges, "2 starred"), search filters both source + translation halves, no-results+Clear, per-item detail (translation/IPA/meaning/From) + Unstar (count 2→1). 1806 tests, 100% gated coverage. Evidence: `dev-docs/verification/feature-22-20260629.md`. Deferred follow-up: the detail "Open in workspace" + IPA-play affordances (need workspace-load/speech wiring). Relates #20 · #3 · #9. GH: #181 · #183 (UI design, closed). |
| 23 | Clear button on the polish input pane (parity with translate source) | VERIFIED | Low | **Shipped + VERIFIED 2026-06-29 (v0.22.0, PR #209, merge `fde7ebe`).** Triage 2026-06-29 ("we need a 'clear' button for polishing too"); the translate source had a Clear, the polish `OriginalCard` had none. NOT a dup of #18 (the goal selector). Design `dev-docs/designs/lucid-polish-clear/` (DesignSync import; closed #199). Plan `dev-docs/plans/20260629-feature-23-polish-clear.md` (v2) — Gate-2 PASSED (2 rounds: r1 1 Med [`onChange('')` → `armPolish` would fire a debounced LLM re-polish on Clear; reset path verified — `reset` aborts + runId-guards] → v2 dedicated non-arming `clearOriginal` + `onClear` → r2 0 open). 1 WI: `PolishPanel` `clearOriginal` (no `armPolish`) + an `onClear` prop on `OriginalCard` + a Clear button leading the header right-group (shown on `value.trim()`, refocus, design's `--accent-ink` ring; resting `--text-tertiary`→`--text-color` like the translate Clear) + `polish.clear` i18n. Gate-4 ship-as-is. **Gate-5 acceptance PASS** — token-free single-origin `@lucid/server` + headless-Chrome CDP: Clear hidden when empty → shown after typing → click wipes the Original (`"…初稿。"→""`) + hides + refocuses; the M1 no-LLM-on-Clear guard via test/audit. 1831 tests, 100% gated coverage. Evidence: `dev-docs/verification/feature-23-20260629.md`. Relates translate Clear · #18 (distinct). GH: #198 · #199 (design, closed). |
| 24 | Starred word-detail affordances — Open-in-workspace + IPA play (deferred from #22) | VERIFIED | Low | **Shipped + VERIFIED 2026-06-29 (v0.21.0, PR #205, merge `662a675`).** Deferred from #22; the committed #22 design's word-detail depicts both (rule 51 satisfied, no new design). User chose **build both** — "Open in workspace" loads the item's source into the translate editor. Plan `dev-docs/plans/20260629-feature-24-starred-detail-affordances.md` (v3) — Gate-2 PASSED (2 rounds: r1 2 High [`setSource`→`onSourceChange` reset path; `Workspace`-level `LOAD_SOURCE_EVENT` chrome consumer] + 3 Med → r2 1 Med [mount-listener stale-closure → ref-to-latest-handler] → 0 open). 1 WI: `src/lib/workspace/loadSource.ts` (event + `onLoadSource`) + `TranslatePanel`/`Workspace` listeners + the two `StarredView` detail buttons (Speak reuses #20 `createSpeech`, novoice/disabled, cancel-on-unmount) + i18n. Gate-4 ship-as-is. **Gate-5 acceptance PASS** — token-free single-origin `@lucid/server` + headless-Chrome CDP: the starred word detail renders Speak + From + Open-in-workspace; **"Open in workspace" loads the source into the translate editor** (`""→"pipeline"`, result reset); armed/disarmed auto-run proof + Workspace pane-switch via tests/audit. 1823 tests, 100% gated coverage. Evidence: `dev-docs/verification/feature-24-20260629.md`. Relates #22 · #20 · `openSettings`. GH: #202. |
| 25 | Read a recorded session task (view its source + result) | VERIFIED | Medium | **Shipped + VERIFIED 2026-06-30 (v0.23.0, PR #216, merge `676129a`).** Triage 2026-06-29 ("sessions are restored but cant be read"); `Task` stored `sourceText`+`resultText` + synced, but `TaskRow` was a display-only `<div>` — restored tasks unreadable. Design `dev-docs/designs/lucid-session-task-read/` (closed #212) — a read-only task detail mirroring the Starred detail (#22) + a row Open-in-workspace reusing #24 `loadSource`. Plan `dev-docs/plans/20260630-feature-25-session-task-read.md` (v3) — Gate-2 PASSED (2 rounds: r1 4 High [sync allow-list would clobber new fields → sync IN scope; langs threaded via panels; sibling buttons not button-in-button; no `PERSIST_VERSION` bump] + 3 Med → r2 1 Med [`resolveBidiDirection` not `detectDirection`] → 0 open). 4 WIs: WI-1 Task optional metadata + capture chain; WI-2 sync round-trip (`seed.ts`/`reconstruct.ts`, new `isOptNonNegInt`/`isOptStringArray` guards); WI-3 `TaskReadView` (render-only); WI-4 `SessionsView` interactive row (2 sibling buttons) + read-view layer. Gate-4 ship-as-is. **Gate-5 acceptance PASS** — token-free single-origin `@lucid/server` + headless-Chrome CDP: open a session → translate read view (Source+Result, **中→EN · 1.5s** captured metadata), polish read view (Original+Polished+**Keywords kept**), row `↗` loads the source into the editor (`""→"在推理阶段…"`) without opening the read view (stopPropagation). 1867 tests, 100% gated coverage. Evidence: `dev-docs/verification/feature-25-20260630.md`. Open-in-workspace lands in translate (polish→Original is a follow-up). Relates #22 · #24 · #19/#3 · #14 · #9/#21. GH: #211 · #212 (design, closed). |
| 26 | Editor sizing — tighter resting height for short content (revisit #13) | PLANNED | Low | Triage 2026-06-30 ("the input box is expanding with the increase of characters"); a short/one-line editor floats over ~33px of dead space. **Design #219 landed 2026-07-09** (`dev-docs/designs/lucid-editor-resting-height/`, imported via the design MCP) and **resolves the direction**: it KEEPS #13's grow-to-content model and only lowers the resting minimum (textarea `min-h-[88px]`→`min-h-[56px]`, polish card `min-h-[130px]`→`min-h-[98px]`) via one shared constant — NOT the "fixed-height / no-grow" reading the earlier brief had drifted to (GH #218's own title always said "tighter resting height"). Grow-to-content + the ~88vh / 50vh-phone caps unchanged. Plan `dev-docs/plans/20260709-feature-26-editor-resting-height.md` — Gate-2 Codex (gpt-5.5/high) **READY TO BUILD**, 0 Crit/High. 1 WI (shared `src/lib/editor/editorSizing.ts` + wire into the 3 editors). Route via `/feature-workflow`. Relates #13 (auto-expanding editors) · #16/#17 (tier caps). GH: #218 · #219 (design, landed). |
| 27 | Clear button on the DRAFT TO POLISH box (parity with #23's ORIGINAL Clear) | TODO | Low | Triage 2026-07-03 (screenshot: the DRAFT box has draft text with no way to clear it). Feature #23 (v0.22.0) added a Clear to the polish **ORIGINAL** box (`OriginalCard.tsx:57`), but the **DRAFT TO POLISH** box (`DraftCard.tsx` — the primary "translation · editable" polish input) has **none**: its header holds only "Translate original" + LookupToggle + LanguagePicker (`DraftCard.tsx:67-89`). **Scope:** a Clear on the DRAFT header — same control as #23 (borderless, shown when non-empty, wipes the draft + resets the polish op non-arming like `clearOriginal`, refocuses); completes Clear parity across both polish inputs. **UI is rule-51 design-gated** — the DRAFT header + Clear isn't in the #23/#199 bundle (that showed ORIGINAL only), and the DRAFT header is busier → **BLOCKED: needs-design (#226)**. Route via `/feature-workflow`. Relates #23 (ORIGINAL Clear). GH: #225 · #226 (design). |
| 28 | Same-origin LLM proxy in @lucid/server (reach CORS-less / local endpoints from the browser) | VERIFIED | Medium | The deeper fix for bug #10 (#228): a browser can't reach a custom/local `http://` endpoint directly (mixed-content / Chrome PNA — CORS is already `*`). **Shipped v0.24.0:** a streaming passthrough proxy in `@lucid/server` — the browser calls the same-origin `/proxy`, the server does the CORS-less/private-IP fetch. Plan `dev-docs/plans/20260706-feature-28-same-origin-llm-proxy.md` (v3, Gate-2 2 rounds → 0 open: capability/direct-by-default; token-free single-origin; `redirect:'error'`; bare-path auth; buildable server coverage). **3 WIs:** WI-1 server (`GET /proxy` capability + auth-gated `POST /proxy` relay, env allow-list `PROXY_ALLOWED_UPSTREAMS`, `redirect:'error'`, streaming; `proxy.ts`+route 100%-gated into `check:all`); WI-2 client plumbing (`proxyRoute.ts` + `ProviderConfig.proxy` + `openaiCompatibleStream` branch + cached allow-list); WI-3 wire `usePanelRun`+`useTestConnection` + privacy copy. **Direct-by-default → zero regression** (empty env / unlisted / token-set → today's path). Gate-4 ship-as-is (SSRF full-URL match + `redirect:'error'` + default-403; bare-path auth; keys forwarded-not-logged; L1 `Retry-After` forwarding applied). **Verified** (real @lucid/server + real mock upstream + real Chrome): direct cross-origin fetch → `TypeError: Failed to fetch` (bug #10) while same-origin `/proxy` streams the upstream SSE; unlisted → 403 no-echo; keys 0-logged. Evidence: `dev-docs/verification/feature-28-20260706.md`. Transparent, not design-gated. Relates bug #10 (#228) · #19 · #7/#10. GH: #232. |
