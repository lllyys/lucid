# Features

Feature tracker for lucid. Lifecycle: `TODO → PLANNED → IN PROGRESS → DONE → VERIFIED`
(see `.claude/rules/47-feature-workflow.md`). One row per feature.

| ID | Title | Status | Priority | Notes |
|----|-------|--------|----------|-------|
| 1 | Project scaffold — Vite + React 19 + TypeScript + Tailwind v4 + shadcn/ui + Zustand + Vitest, wired to a `pnpm check:all` gate | VERIFIED | High | The app foundation: build tooling + the LLM provider layer (Anthropic, behind the single interface) + config store + i18n, behind a minimal placeholder shell. All 7 WIs merged (v0.0.1 → v0.1.0). Plan: `dev-docs/plans/20260614-feature-1-project-scaffold.md` (Gate 2: 3 Codex rounds). Verified: `dev-docs/verification/feature-1-20260614.md`. GH: #1 |
| 2 | Lucid Workspace UI — translate + polish product surface (header + main-toolbar provider switcher, translate pane, polish pane with result/diff); no sidebar (→ #3) | VERIFIED | High | Shipped in v0.2.0 — all 9 WIs committed. Plan: `dev-docs/plans/20260614-feature-2-workspace-ui.md` (v5). Gate 2 closed via rule-47 accept (3 Codex rounds `019ec6a0` → `019ec6bb`). Mock-verified (rule 65 §8); 352 tests, 100% coverage. Verified: `dev-docs/verification/feature-2-20260615.md`. Undesigned surfaces → needs-design #13–#18; real-key e2e blocked on #13. Sidebar → #3. GH: #11 |
| 3 | Sessions & Glossary sidebar — data layer (sessions/tasks history, glossary store, persistence) split out of #2 | VERIFIED | Medium | Shipped in v0.4.0 — all 7 WIs merged (PR #24). Plan: `dev-docs/plans/20260615-feature-3-sidebar.md` (v2). Gate 2: independent subagent audit (Codex rate-limited; rule-48 fallback) — r1 NEEDS REVISION → v2, r2 READY TO BUILD. Gate 4: 4-subagent audit → `.claude/codex-audits/feat-feature-3-sidebar-audit.md` (3 High focus fixes + Mediums resolved; persistence/security CLEAN). 498 tests, 100% coverage. Verified: `dev-docs/verification/feature-3-20260615.md`. Full sidebar variant only (shell/hidden = prototype-only dock; product collapse is future needs-design). GH: #19 |
| 4 | Lucid Workspace — designed gap surfaces: provider Settings/API-key entry (#13), error/cancelled/partial banner (#14), diff Reject + per-hunk accept (#15), dark theme + token table (#16), RTL + direction override (#17) | VERIFIED | High | Shipped in v0.3.0 — all 7 WIs merged (PR #22). Plan: `dev-docs/plans/20260615-feature-4-gap-surfaces.md` (v4); Gate 2: 3 Codex rounds (`019ec907`→`019ec934`, v4 accepted at ceiling). Gate 4: 3 Codex rounds (`019ec974`→`019ec98c`) → `.claude/codex-audits/feat-feature-4-gap-surfaces-audit.md` (bidi converged on authoritative UCD data; dark-AA fixed). Mock-verified (rule 65 §8); 425 tests, 100% coverage. Verified: `dev-docs/verification/feature-4-20260615.md`. #13 lifts mock-only (real key via Settings). Designs: #13–#17. GH: #21 |
| 5 | Multi-provider support — implement OpenAI / Gemini / local·Ollama behind the existing `LLMProvider` interface | IN PROGRESS | High | Umbrella feature folding in #6 (test connection), #7 WI-4 (custom endpoint field), #29 (Settings redesign) — all design-unblocked by PR #39. Plan: `dev-docs/plans/20260615-feature-5-multi-provider.md` (v2, Gate-2 READY TO BUILD; spine verified, 2 High + 4 Med + 3 Low fixed). 8 WIs. **WI-1 (registry data for openai/gemini/ollama — real IDs, allowAnyModel, dormant until WI-4)** done. Gate 4: `.claude/codex-audits/feat-feature-5-wi-1-registry-audit.md` (CLEAN). 534 tests, 100% coverage. Per-vendor keys (in-memory, rule 65 §5). Triage 2026-06-15 ("can't add/test a provider"). GH: #27 |
| 6 | Verify API key / test connection — confirm an entered key actually authenticates | TODO | Medium | Settings only does a client-side shape check (`validateKeyShape`); a bad key surfaces only later as the `invalidKey` banner. A "Test connection" action should make a minimal authenticated probe via the provider interface and report valid/invalid/unreachable inline (rule 65: no key logging, real call so guard cost/timeout, never in `check:all`). Applies to Anthropic + each provider from #5. Triage 2026-06-15 ("can't add/test a key"). GH: #28 |
| 7 | Custom / OpenAI-compatible provider — user-defined base URL + model + key | IN PROGRESS | High | **★ TOP PRIORITY. Engine landed (v0.4.1, PR #35):** WI-1 `openaiCompatibleStream` (generic chat/completions + SSE), WI-2 `custom` vendor + registry (`allowAnyModel`, user-supplied model), WI-3 factory + store (`baseUrl`, in-memory) + presentation (`custom` excluded from the switcher until its config UI ships). Plan: `dev-docs/plans/20260615-feature-7-custom-provider.md` (v2, Gate-2 round-1 fixes). Gate 4: `.claude/codex-audits/feat-feature-7-custom-provider-audit.md` (2 independent Explore subagents, both CLEAN). 525 tests, 100% coverage. **WI-4 (Settings base-URL/model fields) BLOCKED: needs-design (#29)** per rule 51 — feature stays IN PROGRESS (not DONE/VERIFIED) until #29's design loop ships WI-4. Engine shared with #5 (OpenAI/Ollama at fixed URLs). Triage 2026-06-15 ("we need the custom provider"). GH: #31 |
| 8 | Persist polish keywords across reloads | VERIFIED | Medium | Shipped in v0.5.0 (PR #37, merge `79457a9`). `polishKeywordsStore` persists globally via the same crash-proof `safeJSONStorage` as sessions/glossary (`lucid.keywords`, version 1, migrate + partialize) — one keyword set across sessions (global chosen over per-session to avoid a cross-feature `sessionStore` import). Plan: `dev-docs/plans/20260615-feature-8-persist-keywords.md` (v2; Gate 2 READY TO BUILD after fixing a false localStorage-exists premise). Gate 4: `.claude/codex-audits/feat-feature-8-wi-1-persist-keywords-audit.md` (independent subagent, CLEAN/ship-as-is). 528 tests, 100% coverage. Verified: `dev-docs/verification/feature-8-20260615.md` (compositional persistence verification; live-browser reload deferred per #2/#3/#4 precedent). Triage 2026-06-15 ("persist … key words too"). GH: #33 |
